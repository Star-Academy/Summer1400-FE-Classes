// numbers
logSeparator('numbers');
const numbers = [4, 8, 15, 16, 23, 42];
console.log(numbers);

// forEach
logSeparator('forEach');

// filter
logSeparator('filter');

// map
logSeparator('map');

// reduce
logSeparator('reduce');
